# build / deps
.gradle/
build/
out/
# IDE
.idea/
*.iml
# gradle wrapper noise / caches (anchored to root — gradle/wrapper/ below must stay tracked)
/caches/
/daemon/
/native/
/wrapper/
# gradle 임시 파일 (bootRun 중 생성)
.tmp/
# annotation-processor generated sources
src/main/generated/

# 로컬 환경변수 파일 (공개 리포 — 절대 커밋 금지)
.env
.env.*
!.env.example
